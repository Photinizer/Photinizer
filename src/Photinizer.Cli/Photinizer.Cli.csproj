<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <OutputType>Exe</OutputType>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeSymbols>false</IncludeSymbols>
    <DevelopmentDependency>true</DevelopmentDependency>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.CommandLine" Version="2.0.4" />
  </ItemGroup>

  <ItemGroup>
    <!-- main -->
    <None Include="$(OutputPath)Photinizer.Cli.dll"               Pack="true" PackagePath="tools/$(TargetFramework)/any" />
    <None Include="$(OutputPath)Photinizer.Cli.runtimeconfig.json" Pack="true" PackagePath="tools/$(TargetFramework)/any" />
    <None Include="$(OutputPath)Photinizer.Cli.deps.json"         Condition="Exists('$(OutputPath)Photinizer.Cli.deps.json')" Pack="true" PackagePath="tools/$(TargetFramework)/any" />

    <!-- ensure System.CommandLine.dll is present -->
    <None Include="$(OutputPath)System.CommandLine.dll"
          Condition="Exists('$(OutputPath)System.CommandLine.dll')"
          Pack="true" PackagePath="tools/$(TargetFramework)/any" />

    <!-- other managed dependencies -->
    <None Include="$(OutputPath)*.dll"
          Exclude="$(OutputPath)Photinizer.Cli.dll;$(OutputPath)System.CommandLine.dll"
          Pack="true" PackagePath="tools/$(TargetFramework)/any" />

    <!-- native/runtime assets -->
    <None Include="$(OutputPath)*.so;$(OutputPath)*.dylib"
          Pack="true" PackagePath="tools/$(TargetFramework)/any" />

    <!-- export CLI path transitively -->
    <None Include="buildTransitive\Photinizer.Cli.props" Pack="true" PackagePath="buildTransitive\" />
  </ItemGroup>

</Project>
